# Restoring the Home screen
1. mv app-home ../../app/home ; mv api-posts-media ../../app/api/posts/media
2. mv mediaScreen.ts ../../services/ ; mv 009_home_media.sql ../../lib/migrations/
3. Run migration 009; create Supabase bucket "media" (public read, auth write, 10MB cap)
4. Re-add nav link in app/layout.tsx
Gate (set at deferral): restore only with post-launch evidence of demand for identity expression
beyond discussion. Video remains a separate decision (streaming pipeline + moderation costs).
